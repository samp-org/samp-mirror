services:
  mirror:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    command:
      - "--node"
      - "ws://host.docker.internal:9944"
      - "--db"
      - "/data/mirror.db"
      - "--port"
      - "8080"
    restart: unless-stopped
